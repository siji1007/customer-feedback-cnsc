import React, { useState } from 'react';

const CustomerBtn: React.FC = () => {

    return (
        <div>
           <h1>This is the CUSTOMER BUTTONS PAGE</h1>
        </div>
    );
};

export default CustomerBtn;
